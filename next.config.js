/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
  images: {
    domains: ['https://iqhobruxykflracjmimx.supabase.co'],
  },
}

module.exports = nextConfig
